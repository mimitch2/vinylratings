import React, { useState } from 'react';

import { VRContainer, VRText } from 'components';

const Settings = () => {
    return (
        <VRContainer>
            <VRText>Settings</VRText>
        </VRContainer>
    );
};

export default Settings;
